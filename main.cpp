#include <stdio.h>
#include <string.h>
#include <time.h>
#include <stdlib.h>
int main(void)
{
	//srand(time(NULL));
	int a = rand() % 1000;
	//int b = rand() % 1000;
	//int c = rand() % 1000;
	printf("%d %d %d", a, b, c);
	printf("Hello World");
    return 0;
    
}
